<Controls:MetroWindow
         x:Class="SunriseClock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SunriseClock"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="Sunrise Clock" 
        Padding="30"
        GlowBrush="{DynamicResource AccentColorBrush}"
        WindowStartupLocation="CenterScreen">

    <DockPanel DataContext="{Binding Configuration.Alarms}">

        <ListBox x:Name="listBox" ItemsSource="{Binding}" Style="{StaticResource AlarmsListStyle}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Style="{StaticResource AlarmDetailStyle}">
            <Label Content="Wecker Name" />
            <TextBox Controls:TextBoxHelper.Watermark="Wecker Name" Text="{Binding Name}" />
            <Label Content="Weckzeit" />
            <TextBox Controls:TextBoxHelper.Watermark="Weckzeit" />
            <Label Content="Erleuchtungszeit" />
            <TextBox Controls:TextBoxHelper.Watermark="Erleuchtungszeit" />
            <Label Content="Lichtdauer" />
            <TextBox Controls:TextBoxHelper.Watermark="Lichtdauer" />
            <Controls:ToggleSwitch Header="Aktiviert" />
            <TextBox Controls:TextBoxHelper.Watermark="This is a textbox" />

            <Label Content="Wochentage" />
            <StackPanel Orientation="Horizontal">
                <CheckBox />
                <CheckBox />
                <CheckBox />
                <CheckBox />
                <CheckBox />
                <CheckBox />
                <CheckBox />
            </StackPanel>

            <StackPanel Style="{StaticResource ButtonToolbarStyle}">
                <Button Style="{DynamicResource CircleButtonStyle}" Command="{Binding ConfigurationSaveCommand}">
                    <iconPacks:PackIconFontAwesome Width="20" Height="20" Kind="FloppyOutline" />
                </Button>
                <Button Style="{DynamicResource CircleButtonStyle}">
                    <iconPacks:PackIconFontAwesome Width="20" Height="20" Kind="Plus" />
                </Button>
                <Button Style="{DynamicResource CircleButtonStyle}">
                    <iconPacks:PackIconFontAwesome Width="20" Height="20" Kind="Cog" />
                </Button>
                <Button Style="{DynamicResource CircleButtonStyle}">
                    <iconPacks:PackIconFontAwesome Width="20" Height="20" Kind="Trash" />
                </Button>
            </StackPanel>
        </StackPanel>
    </DockPanel>



    <!-- Configuration -->
    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <ToggleButton Content="Configuration" IsChecked="{Binding ElementName=configurationFlyout, Path=IsOpen}" Cursor="Hand"/>
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>

    <Controls:MetroWindow.Flyouts>
        <Controls:FlyoutsControl>
            <Controls:Flyout x:Name="configurationFlyout" Header="Configuration" Position="Right" Width="200">
                <Border Padding="10">
                    <StackPanel VerticalAlignment="Top">
                        <Label Content="Hostname"/>
                        <TextBox Controls:TextBoxHelper.Watermark="Hostname" />
                        <Label Content="Port"/>
                        <TextBox Controls:TextBoxHelper.Watermark="Port" />
                    </StackPanel>
                </Border>
            </Controls:Flyout> >
        </Controls:FlyoutsControl>
    </Controls:MetroWindow.Flyouts>






</Controls:MetroWindow>
